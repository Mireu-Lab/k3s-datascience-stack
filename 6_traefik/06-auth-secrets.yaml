---
# Spark Basic Auth Secret
# Default credentials: admin / spark@mireu
# To generate new password: htpasswd -nb username password
# Note: Store the htpasswd output directly in stringData, not base64 encoded
apiVersion: v1
kind: Secret
metadata:
  name: spark-auth-secret
  namespace: spark-operator
type: Opaque
stringData:
  users: |
    admin:$apr1$TPZD9LUW$hn0o9y7YUD3klmrvARq.z/
---
# HDFS Basic Auth Secret
# Default credentials: admin / hdfs@mireu
# To generate new password: htpasswd -nb username password
# Note: Store the htpasswd output directly in stringData, not base64 encoded
apiVersion: v1
kind: Secret
metadata:
  name: hdfs-auth-secret
  namespace: default
type: Opaque
stringData:
  users: |
    admin:$apr1$4YQKVm/J$FEZvQm5vKwfVHJG3wDxBa.
